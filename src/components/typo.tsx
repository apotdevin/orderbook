import { Colors } from 'src/styles/Colors';
import styled from 'styled-components';

export const Typo = {
  bid: styled.div`
    color: ${Colors.green};
  `,
  ask: styled.div`
    color: ${Colors.red};
  `,
};
